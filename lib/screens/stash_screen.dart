import 'package:flutter/material.dart';

class StashScreen extends StatelessWidget {
  const StashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Estoque aqui....');
  }
}